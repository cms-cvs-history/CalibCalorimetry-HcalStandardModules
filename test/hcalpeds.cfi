module analpeds = HcalPedestalAnalyzer {
# main outputs
    untracked string outputPedestalsDest = 'peds.xml'
# if txt output, need a separate output file for widths
#    untracked string outputPedestalWidthsDest = 'widths.txt'
# output histo file
    untracked string outputFileHist = 'peds.root'
# flag for channel by channel histograms
    untracked int32 hiSaveflag = 0
# to divide input data into samples of ... events
    untracked int32 nevtsample = 9999999
# to have pedestals in ADC (default: fC)
    untracked int32 pedsinADC = 0
# flag for pedestal validation (if 1, need also to define input pedestals)
    untracked int32 pedValflag = 0
#    untracked string inputPedestalsSource = 'peds_ref.txt'
#    untracked string inputPedestalWidthsSource = 'widths_ref.txt'
}
